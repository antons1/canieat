const React = require('react');

const Header = (props) =>
    <header className="top-bar main-header">
        <div className="top-bar-left">
        </div>
        <div className="top-bar-right">

        </div>
        <div className="row"><h1>Can I eat?</h1></div>
    </header>;

module.exports = Header;
